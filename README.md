# self-checkout
Grocery-bagging puzzle game

## Dev
- `npm test` — run the headless logic assertions.
- `npm run serve` — start a static server, then open <http://localhost:8000>. ES modules require HTTP; `file://` won't work.
